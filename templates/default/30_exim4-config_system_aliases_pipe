# This transport is used for handling pipe deliveries generated by /etc/aliases

system_aliases_pipe:
  debug_print = "T: system_aliases_pipe for $local_part@$domain"
  driver = pipe
  ignore_status
  log_output
  user = daemon
  group = daemon
